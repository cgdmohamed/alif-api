NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=alef
DB_PASSWORD=alef
DB_DATABASE=alef_dev
# Keep true until real migrations exist (see README's Production notes).
DB_SYNCHRONIZE=true

JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
JWT_REFRESH_EXPIRES_IN=30d

CORS_ORIGIN=http://localhost:5173

# Agora (https://console.agora.io -> Project Management -> create a project).
# Leave unset to use the local MockAgoraProvider instead (see MeetingsModule).
AGORA_APP_ID=
AGORA_APP_CERTIFICATE=
