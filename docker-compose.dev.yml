services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: alef
      POSTGRES_PASSWORD: alef
      POSTGRES_DB: alef_dev
    ports:
      - '5432:5432'
    volumes:
      - alef_pg_data:/var/lib/postgresql/data

volumes:
  alef_pg_data:
